"""
## Problem 2 — 2D Vector with __add__, __sub__, __mul__, __str__

### Requirements

Class Vector2D:
   __init__(self, x, y)
   __str__: Returns "Vector2D(<x>, <y>)"
   __add__(self, other): Returns new Vector2D(self.x + other.x, self.y + other.y)
   __sub__(self, other): Returns new Vector2D(self.x - other.x, self.y - other.y)
   __mul__(self, scalar): Returns new Vector2D(self.x * scalar, self.y * scalar)
   __eq__(self, other): Returns True if both x and y match.

### Sample Run
v1 = Vector2D(3, 4)
v2 = Vector2D(1, 2)

print(v1)               # Vector2D(3, 4)
print(v1 + v2)          # Vector2D(4, 6)
print(v1 - v2)          # Vector2D(2, 2)
print(v1 * 3)           # Vector2D(9, 12)
print(v1 == Vector2D(3, 4))   # True

----------------------------------------------------------------------
Write your solution below. Refer to solutions.md only when stuck.
----------------------------------------------------------------------
"""

# Write your solution here:

